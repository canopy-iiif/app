import {Stylesheet} from "@canopy-iiif/app/head";

export function Head() {
  return (
    <>
      <Stylesheet />
      <meta name="theme-color" content="#111" />
    </>
  );
}

export function App({ children }) {

return (

  <div className="bg-white text-slate-800">
    <header>
      <div className="px-4 py-3 flex items-center justify-between gap-4">
        <a href="/" className="flex items-center gap-2 ">
          <img src="/canopy.svg" alt="Canopy IIIF" style={{
            height: '1.5rem',
          }} />
          <span>Canopy IIIF</span>
        </a>
        <SearchPanel label="Search" hotkey="mod+k" placeholder="Search…" />
        <nav className="flex items-center gap-4">
          <a href="/search?type=work">Works</a>
          <a href="/docs">Docs</a>
          <a href="/docs/about">About</a>
        </nav>
      </div>
    </header>

    <main className="max-w-none">
      {children}
    </main>

    <footer className="border-t border-slate-200 text-slate-500">
      <div className="mx-auto max-w-content w-full px-4 py-6">(footer)</div>
    </footer>

  </div>

);
}
