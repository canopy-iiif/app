import {MDXProvider} from "@mdx-js/react";
import {
  DocsCodeBlock,
  DocsMarkdownTable,
  Interstitials,
} from "@canopy-iiif/app/ui/server";

export const canopyVersion =
  (typeof process !== "undefined" &&
    process.env &&
    (process.env.CANOPY_APP_VERSION ||
      process.env.npm_package_version ||
      process.env.npm_package_VERSION)) ||
  "dev";

<>
  <Interstitials.Hero variant="text" description={`v${canopyVersion}`} />
  <Layout navigation={true} fluid={true}>
    <MDXProvider components={{pre: DocsCodeBlock, table: DocsMarkdownTable}}>
      {props.children}
    </MDXProvider>
  </Layout>
</>
