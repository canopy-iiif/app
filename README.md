# Canopy IIIF has moved

This project moved from `canopy-iiif/app` to [`nulib-ds/canopy`](https://github.com/nulib-ds/canopy). Its documentation site now lives at **https://nulib-ds.github.io/canopy/**.

This repository exists only to redirect every URL that was previously hosted at `https://canopy-iiif.github.io/app/*` to its new location, so existing links and bookmarks keep working.
